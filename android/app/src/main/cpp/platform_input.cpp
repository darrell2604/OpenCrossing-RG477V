#include "port_controller.h"

// Android input is translated into the shared controller state through the
// port_controller API. The state itself is implemented in port_controller.cpp;
// this translation unit is intentionally kept free of duplicate definitions.
